# Runtime dependencies for local dev + unit tests. On Databricks Serverless env v4,
# pyspark / mlflow / delta / hyperopt are already provided by the runtime — do not
# pin them here or you will fight the runtime.
holidays>=0.50
pandas>=2.0
pytz>=2024.1
requests>=2.31

# dev / test
matplotlib>=3.8   # src/plotting.py — ships with the Databricks runtime, pinned here for local dev
pytest>=8.0
